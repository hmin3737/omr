# Vercel Prisma Postgres 통합이 자동으로 주입하는 연결 문자열.
# 로컬 개발 시에는 Vercel 대시보드(Storage → 해당 DB → .env.local)에서
# 값을 복사하거나 `vercel env pull` 로 받아 아래에 채운다.

DATABASE_URL="postgres://USER:PASSWORD@HOST/DB?sslmode=require"
